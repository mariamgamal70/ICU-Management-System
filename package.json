{
  "name": "final database",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "dev": "parcel ./src/*.html",
    "prebuild": "npx rimraf build",
    
    "build": "parcel build --public-url ./ ./src/*.html --no-minify --out-dir build"
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.0"
  },
  "devDependencies": {
    "cssnano": "^5.1.13",
    "postcss": "^8.4.16",
    "sass": "^1.54.4"
  }
}